% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/FADN2Footprint_helpers.R
\name{h_create_dictionary_template}
\alias{h_create_dictionary_template}
\title{Create a template Excel file for national-to-EU FADN dictionary}
\usage{
h_create_dictionary_template(
  output_path = "dictionary_conversion.xlsx",
  raw_tables,
  id_cols = "IDENT",
  table_config,
  data_extra,
  national_code_col = "RICA_code_number",
  overwrite = FALSE
)
}
\arguments{
\item{output_path}{Character. File path for the output Excel workbook.}

\item{raw_tables}{Named list of data frames — the raw national FADN tables.}

\item{id_cols}{Character vector. Column name(s) identifying individual farms
(e.g., \code{c("IDENT")}). These columns are preserved as-is in the output
and used as grouping keys during pivoting.}

\item{table_config}{Named list mapping each product group to its table name
and code column. Names must be \code{"CROPS"}, \code{"LIVESTOCK"},
\code{"ANIMAL PRODUCTS AND SERVICES"}. Each element is a list with
\code{table} (name in \code{raw_tables}) and \code{code_col} (column name
holding the product code).}

\item{data_extra}{List with elements \code{crops}, \code{livestock},
\code{output}, each containing columns \code{FADN_code_letter} and the
column named by \code{national_code_col}.}

\item{national_code_col}{Character. Column name in \code{data_extra} tables
that holds the national product codes.}

\item{overwrite}{Logical. Overwrite existing file? Default \code{FALSE}.}
}
\value{
Invisibly returns the output path.
}
\description{
Generates a pre-populated Excel workbook the user must fill to define the
mapping between a national FADN and the EU FADN standard. The workbook
contains a \code{variable_pattern} sheet (suffix mapping that drives the
pivot), a \code{category_pattern} sheet (product code mapping), a
\code{manual_matches} sheet (farm-level variables), and reference sheets.
}
\concept{utils}
