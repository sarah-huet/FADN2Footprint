% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/FADN2Footprint_helpers.R
\name{h_generate_mock_data}
\alias{h_generate_mock_data}
\title{Generate mock FADN data from descriptive statistics}
\usage{
h_generate_mock_data(
  n = 500,
  continuous_stats = NULL,
  categorical_stats = NULL,
  group_cols = NULL,
  group_weights = NULL,
  seed = 42
)
}
\arguments{
\item{n}{Total number of rows to generate.}

\item{continuous_stats}{Data.frame with columns: variable, mean, sd, min, max,
and optionally grouping columns (e.g., farm_type, country).}

\item{categorical_stats}{Data.frame with columns: variable, level, count,
and optionally the same grouping columns.}

\item{group_cols}{Character vector of column names used for grouping
(e.g., c("farm_type", "country")). NULL = no grouping.}

\item{group_weights}{Named list or data.frame defining how to split n across
groups. If NULL, rows are split equally across groups.
As data.frame: must contain group_cols + a "weight" or "count" column.}

\item{seed}{Random seed.}
}
\value{
A data.frame.
}
\description{
Handles three cases:
  1. General stats (no grouping) → one block of n rows
  2. Stats grouped by one or more factors (e.g., farm_type, country)
     → generates rows per group proportionally or with explicit counts
}
